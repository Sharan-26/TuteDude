-- Individual

git add TD_Assignment1.py
git status
git commit -m "TDAssignment1"
git push

-- All

git add .
git commit -m "Add all new assignments and updates"
git pull --rebase origin main
git rebase --continue
git push origin main


-- Removing all except to keep one
git checkout main
git rm -r --cached .
git add Assignment1.py
git commit -m "Keep only Assignment1.py and remove all other files"
git push origin main

Optional one
git clean -fd
